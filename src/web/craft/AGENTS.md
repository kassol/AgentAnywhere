# Craft 组件迁移

## 职责

本目录保存从 Craft Agents OSS v0.13.3（commit `e8963854c3679edcceb105a42537a06749e6cb64`）迁入的前端组件，以及为 AgentAnywhere 现有接口所做的最小适配。

## 目录结构

- `components/`：导航、输入、轮次、工具活动、工作介入、报告阅读和设置组件。
- `components/annotations/`：原文选择、定位、浮层交互与批注草稿持久化边界。
- `lib/`：迁入组件共享的纯前端工具。
- `styles.css`：Craft 组件使用的 Tailwind 主题映射与局部样式。

## 模块规范

- 每个迁入文件保留上游版本、原路径、版权和许可证说明。
- 保留使用中的原函数和 JSX 结构。裁剪 Electron、附件等范围外功能时，在文件头记录差异；实际使用链和裁剪依据统一维护在 `docs/r4-craft-source.md`。
- 组件只接收现有页面传入的数据和回调。不得在本目录新增业务 API、数据库字段或并行状态机。
- 使用相对 import。公共组件从原组件名文件导出。
- 富文本输入保持纯文本提交、粘贴转义、撤销、光标、IME 和长度上限行为。发送成功后的草稿清理由现有 Composer 状态机决定。

## 依赖关系

本目录可依赖 React、Tailwind、Motion、Lucide、Radix 及通用 UI 工具。业务页面通过 props 提供安全 Markdown 渲染器和现有领域数据。

## 变更日志

- 2026-09-20：建立 Craft 核心组件迁移边界。
- 2026-09-20：补齐工作、批注、设置与来源维护边界。
