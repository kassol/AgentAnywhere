# R2-08：保留进度重试与明确替换模型

实现提交 `7c425d183059d140ce239c979000cc17afa2ae20`。隔离执行链路、真实浏览器重试及历史回执模型展示复验通过。跨票统一恢复矩阵另见 R2-10。

## 已验证

`test-steward-retry.mjs` 在应用版本 `260dc831ec2a6a7d219bee0a2972d6e65ed11ef5` 通过。Web/API、PostgreSQL、队列、Pi 和 OpenSandbox 为真实组件；外部模型 HTTP 使用可控响应。

- 同模型与兼容替代模型均从真实失败检查点建立新 Run，保持原 Task，不重做检查点前的工具执行。
- 协议及上下文不兼容的替代模型被拒绝，检查点仍可用于后续同模型重试。
- 引用命令与未授权建议不创建 Run；失败解释包含真实失败、有效池及已有成功记录。
- 业务提交后停止、Web 重启及明确恢复返回原回执，不重复创建 Run。测试涉及 Run 均经 SDK 核查无活跃沙箱。

同模型操作 `0f96713f-5959-4303-a828-4e1a3c6c5a24`；替代模型操作 `9677e03f-53cd-4543-bd76-13d0cad40496`；重放回执 `2d2b9e38-178d-4310-9177-e9ca7e47d463`。

## 浏览器

ego-browser TaskSpace 32 中，对话 `6c3b615b-c923-499e-b959-34ef01b39430` 通过键盘输入“同模型重试工作 78429a73-11f4-4ed0-8e2a-45b53c5d3544。”，创建新 Run `de162d6b-04d3-4055-90bf-2dc9d2c4b8c6` 并成功交付、回收。页面显示原 Run 与新 Run。

发现旧 JSON 字符串快照导致回执未显示模型 ID/协议；#28 已兼容读取并修复新写入，已在应用 `6787b70a47a96c8ab9b20056d1d67e3fb0efbbed` 的同一历史对话复验：页面显示 `fixture-retry（chat-completions）`、原 Run `f12566d0` 和新 Run `de162d6b`，截图 `r2-08-retry.png` 已检查。
