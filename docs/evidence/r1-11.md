# R1-11 取消验证

2026-09-18，cc-la `19112/19113` 隔离栈运行 `node infra/r1/test-cancel.mjs`。脚本从公开 API 验证排队取消两次请求的幂等结果，以及 Chat Completions 与 Responses 流式模型执行中的取消；Run 进入 `cancelled/cleaned`，未产生成功完成事件。已有报告后取消仍可读取报告；成功完成后的取消保持 `succeeded`。真实 `search_web` 请求在 HTTP fixture `/waiting-search` 挂起时已有 `tool.started` 而未完成，取消后 fixture 等待数归零，模型请求次数不再增加。无报告的 `ask_user` 检查点交接时取消也进入 `cancelled/cleaned`。上述 Run 经 OpenSandbox SDK 按 ID 查询均无活沙箱。

首轮验收曾抓到检查点 JSONL 被 SDK 限行读取裁掉末尾换行，导致一个无报告测试 Run 进入 `save_failed/blocked`；它由人工在隔离测试 schema 清场，不计取消通过。修复后重新执行完整脚本通过。等待状态已经复制并校验的报告与附件在取消事务中登记为 ArtifactVersion，不依赖已回收的 Sandbox；报告与空附件的公开下载结果见 [R1-10](r1-10.md)。正式服务与数据库未用于该回归。
