# R4-04 管家输入与活动流证据

日期：2026-09-20  
Issue：[#46](https://github.com/kassol/AgentAnywhere/issues/46)  
固定上游：Craft Agents OSS `v0.13.3` / `e8963854c3679edcceb105a42537a06749e6cb64`

## 实现与调用链

- `src/web/craft/components/LoadingIndicator.tsx:12-75` 和 `src/web/craft/styles.css:45-76` 保留上游九格 Spinner、加载文字及 elapsed 结构；`FreeFormInput.tsx:72-105`、`TurnCard.tsx:54-228`、`Steward.tsx:269-275` 分别用于输入核对、活动/回复流式状态和已有对话加载。
- `src/web/craft/components/ActionBar.tsx:13-25` 保留 `AuthCardActions` 的动作区主体；`src/web/QuickActions.tsx:56-76` 用 Craft Button 显示完整动作名与精确 Task/Run/Interaction/Version/Operation 身份。点击仍只填入 `quickActionCommand`，`Steward.tsx:250-255` 继续在覆盖现有草稿前确认。
- `src/web/ActivityFeed.tsx:42-106` 以 `turnId/epoch + toolCallId` 合并真实 tool.started/completed，保留工具名、阶段、参数、结果和错误；`:133-190` 只在 following 状态自动跟随，历史阅读时保持位置并提供“回到最新内容”。
- `src/web/Composer.tsx:17-95,107-125` 继续按 Thread 保存草稿与 pending 请求身份。`Steward.tsx:107-123,178-244` 只在服务端接受同一 requestId 后清 pending；未知发送结果保留原请求，明确 4xx 释放 pending，接受期间出现的新 revision 不会清空。
- `src/web/craft/components/RichTextInput.tsx:141-169,230-295` 修复两个 contenteditable 边界：达到 16000 后在中间插入只裁剪新增段，原后缀不丢；composition input 立即推进草稿 revision，组合期间受控 effect 不覆盖浏览器 DOM。`FreeFormInput.tsx:72` 增加键盘可见焦点环。
- `Steward.tsx:256-264,337-344` 的停止接口只停止当前管家轮次；工作取消仍由带 Task/Run 身份的 QuickAction 触发，两个生命周期保持独立。

## 自动验证

```text
$ bun test src/web/craft/components/RichTextInput.test.ts src/web/Composer.test.ts src/web/ActivityFeed.test.tsx src/web/QuickActions.test.ts src/web/StewardQuickActions.test.ts src/web/StewardReceipts.test.ts
22 pass
0 fail
56 expect() calls

$ bun run typecheck
$ tsc --noEmit
```

`RichTextInput.test.ts:4-25` 覆盖已满草稿头部插入仍保留末尾、部分新增可进入及选择替换。未运行全套 `bun test`，符合本票隔离验证约束。

## ego-browser 真实链路

主线程使用唯一 TaskSpace 46、Chromium CDP、Vite `127.0.0.1:5174` 和隔离 live API `19114` 验证：

- IME composition + Enter/keyCode 229 未发送轮次；提交组合文本后中文完整进入草稿。该路径覆盖真实 Chromium 组合事件，不包含操作系统候选窗手工选择。
- 断网发送保留草稿和 pending requestId `0493d6f7-71f1-4dbb-83a1-c39bc0faf59a`。恢复网络并新增编辑后重试，只产生 Turn `d5ad4caa-84de-4c32-b757-7ac5977bd5c4`；服务端收到原快照和同一 requestId，新增草稿保留，pending 清除。
- 长回复 Turn `126d1ab5-9991-415a-a726-a2c4603039c8` 从 62 增至 1563 字符、scrollHeight 从 1954 增至 2431 时，历史位置 scrollTop 保持 0；“回到最新内容”有效。
- 停止该管家轮次后服务端状态为 `stopped`，已派发的独立 Task 仍为 `succeeded`。当前 Thread 草稿与 new 草稿经切换、刷新后分别恢复。

浏览器在修复前已复现“15999 个甲 + 末尾终，在开头插入新后末尾终丢失”。本轮纯逻辑回归已通过；修复后的真实 DOM 和“服务端接受恰遇在途组合输入”由主线程继续复验并补入最终验收。


### 修复后浏览器复验

- 满 16000 字（15999 个甲 + 终）时在开头插入“新”，DOM 长度保持 16000、末尾仍为“终”，原正文完整。
- 网络延迟设为 2000ms 后发送讨论，在 pending 期间以 Chromium IME 替换输入为“正在组合的新草稿”；服务端接受原 requestId `f14eb97d-c54e-46a7-8f66-08392918a720` 后，候选 DOM、提交组合后的 DOM 和持久草稿均保留该新文字，pending 被清除。
- 两项修复均通过真实浏览器复验；模拟延迟已恢复为 0。操作系统输入法候选窗的人工选择不属于上述自动化结果。

报告预览共用键盘边界也已修复：全局 Esc 关闭处理忽略 composition、229 与已处理事件。浏览器在批注 textarea 组合“批注组合”时发送 Esc/229，报告保持打开；提交候选后普通 Esc 正常关闭。
