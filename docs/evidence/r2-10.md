# R2-10：按持久回执继续操作

实现 `6787b70a47a96c8ab9b20056d1d67e3fb0efbbed` 已通过本地回归、统一隔离故障矩阵和真实浏览器复验。

## 行为

六类操作分别接收完整“继续调研/追加/取消/回答/重试/改稿回执 UUID”命令。页面按钮只填入命令，发送后才创建独立新轮次。已接受操作读取原结果；未执行操作保持原业务身份，并重新核对当前模型池、凭证及工作状态。目标尚未冻结的操作要求重新委托。

回答由服务端从完整用户消息直接解析，避免模型重复转写导致有效回答被拒。控制与调研增加当前消息的正向委托校验，防止引用或否定文本误授权。指定 UUID 的控制、回答、重试和改稿要求目标一致；自然语言控制目标必须唯一匹配。

等待态追加等业务拒绝保留实际失败原因。重试回执兼容历史 JSON 字符串快照，只公开模型 ID 和协议。

## 验证

本地 `steward.test.ts` 与 `work.test.ts`：9 项、245 个断言通过；typecheck、build 与相关脚本语法通过。统一应用测试：21 项、428 个断言通过。独立复核及主线程沿调用链复查后，控制、回答、重试和改稿均补齐精确 Task 绑定。

真实 ego-browser 在同一历史对话复验明确回答与等待态追加拒绝原因；刷新后“填入继续命令”不增加轮次，发送“继续改稿回执”仍返回原 Run，未创建重复执行。普通讨论停止后，已接受的改稿继续完成；历史重开保留新旧成果和回执。三个真实 Run 均经 SDK 查询无活跃 Sandbox，详见 [R2-12 浏览器记录](r2-12.md)。

统一隔离已通过调研、控制、回答、状态、摘要与重试。重试恢复时移除冻结的替代模型，操作保持未执行；恢复原池后使用原操作身份继续，回执 `275173e0-46ef-44a5-a8cd-0b51c29d5ce4`。同模型/替代模型回执分别为 `6261755f-25fe-46c2-bcd7-427b0fbd48e7`、`14f9d2d4-027f-4576-9a6a-1a40b9c31791`；原回执重放 `642c076c-83db-4e19-ab89-9b44ae585648`，未重复执行。改稿最后一组通过：回执 `6accf6c4-53bf-4966-a851-6f321b57c01d` 在业务接受后停止、Web 重启、明确继续，始终返回原 Run `3c4af331-f47b-482f-b2ea-2ec570c6512c`。失败的新执行仍保留两个报告版本，旧版下载哈希一致，SDK 无活跃 Sandbox。

统一隔离入口从完整 Bun 开始执行全部检查；环境 DNS、外部 HTTP 502 和旧测试正文断言修正后，从失败项续跑并保留前项记录。最终全部检查均通过，无跳过项；测试修正提交见 [R2-12](r2-12.md)。
