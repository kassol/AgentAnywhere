# R3-03：真实活动流与阅读保持

对应 #35。核心实现提交 `f44ba6c`，回执轮次修复为 `96202e5`、`1d6362e`；正文与状态投影触发跟随的补强包含 `2964218`、`d95f19e`。浏览器验收使用候选 `08e8ca9` 的独立 PostgreSQL、队列、Pi、OpenSandbox 栈，模型响应来自可控 HTTP fixture。

- `ActivityFeed` 只读取持久 `tool.started`、`tool.completed` 事件，按真实 `turnId` 或 Run `epoch` 与 `toolCallId` 合并。同轮重复事件、先到的完成事件和 500 条分页边界不会产生重复工具卡；没有事件的历史轮次保持为空。
- 真实 Run 工具卡默认折叠，摘要显示工具名称、执行状态和简短结果；展开后显示实际参数与完整结果。管家 `read_frozen_work` 也作为所属轮次的真实工具活动展示。失败结果使用独立状态；待回答 Interaction、稳定操作回执和已保存成果继续来自公开 API 的事实投影。
- 管家回执通过持久 `resumeTurnIds` 同时显示在原轮和同线程恢复轮，仍是同一操作回执；跨线程恢复未开放，也不会投影到其他线程。该路径的真实浏览器证据见 [R3-05](r3-05.md)。
- 工具参数和结果使用纯文本 `pre` 展示；报告与助手 Markdown 继续经过现有安全渲染器。未把工具或历史内容作为授权来源。

ego-browser TaskSpace 34 实测滚动边界：

- `.conversation-messages` 位于末尾时 `scrollTop=3916`、底部间距为 0；新内容到达时继续跟随。
- 上滚 700px 后 `scrollTop=3216`。通过公开 API 实际 POST 新管家轮次并产生新消息后，位置仍为 3216。
- 另一次在 `scrollTop=3436` 刷新页面，等待应用恢复后仍为 3436。
- 模拟 `prefers-reduced-motion: reduce` 后点击“回到最新内容”，底部间距恢复为 0。

聚焦回归共 5 项通过：活动去重与迟到结果、500 条分页、持久结果摘要、同线程恢复回执投影和真实 PostgreSQL 轮次投影；`bun run typecheck` 通过。草稿、停止回复和刷新恢复的输入行为已在 [R3-02](r3-02.md) 验证，本票未重复。

Craft `v0.13.3` 固定来源、Apache-2.0 许可、原组件路径、依赖裁剪及本地事件映射见 [活动流来源记录](../r3-activity-upstream.md)。本票没有单独重复工具卡在 390px 和明暗主题下的截图；整体外壳的小屏和双主题浏览器结果见 [R3-01](r3-01.md)。
