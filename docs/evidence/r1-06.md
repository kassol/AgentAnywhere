# R1-06 Responses 执行验收

2026-09-18，在 cc-la 的隔离 R1 环境验证。Run 快照选择 `responses` 时，沙箱内 Pi 0.85.1 注册 `openai-responses`，向 Web 内部代理发送原生 `/v1/responses` 请求；Web 按 Run 的模型、协议和凭证版本转发到同一路径。Chat Completions 分支保留。设置页连接测试按已保存协议发非流式 Responses 请求。

`node infra/r1/test-run.mjs` 通过公开 Web/API 创建七个串行 Run，仅在外部模型 HTTP 边界使用 fixture。真实 PostgreSQL、pg-boss、Pi 和 OpenSandbox 均参与：三个 Chat、两个 Responses Run `succeeded/cleaned`；Responses 的 HTTP 400 和流式 `response.failed` 分别成为含原错误原因的 `failed/cleaned`。Responses fixture 分段返回工具参数与文本，第二轮请求含 `function_call_output`，未发送 Chat `messages`；公开事件 API 确认工具结果、最终消息、事件去重与游标，已知和缺失用量分别显示数字与未知。SDK 按七个 Run ID 查询均无存活沙箱。完整脚本输出为 `serial=true`、`persistedEvents=true`、`usage=known-and-unknown`、`sandboxes=none-active`。

真实浏览器 TaskSpace31/p1 在隔离 `127.0.0.1:19112` 看到 Responses 的流式中间文本；刷新后为一条工具结果和一条最终消息，输入/输出 20/10 token、实际费用未知。[浏览器截图](r1-06-responses.png)。流式失败场景显示 `fixture_stream_failure: fixture streamed failure` 和未知用量；缺失用量场景完成后输入/输出均显示未知。设置页显示 `fixture-responses` 的 Responses 连接测试成功。

另用独立 `r106_live` schema、`127.0.0.1:19114` Web 和只读挂载的模型配置副本调用真实 sub2api。只在副本中把 `gpt-6-astra` 的协议设为 Responses；连接测试返回成功。Task `61e6690e-fcd7-4d52-878a-35d71be61028`、Run `cb366149-f008-456e-b4a8-e48905442e4c` 完成真实 echo 工具往返，公开 API 读到 `responses`、`succeeded/cleaned`、27 条持久事件；两轮用量分别为输入/输出 4562/36、4613/13 token。沙箱查询无存活资源。测试后已删除 19114 Compose、隔离 schema 和模型配置副本；正式模型仍保存 Chat Completions，正式 R1 服务未切换。

本地 `bun run build`、`bun run typecheck` 通过；主线程补充独立 PostgreSQL 连接后，`bun test` 全部通过（8 pass，161 assertions）。完整隔离脚本覆盖真实数据库、队列、Pi 与沙箱。取消矩阵留给 #14，正式公网发布与最终双协议验收由后续票完成。
