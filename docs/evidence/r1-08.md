# R1-08 私有搜索与公开网页调研验证

2026-09-18，在 cc-la 的隔离 `r1-08` 镜像和测试 PostgreSQL schema 上，`node infra/r1/test-research.mjs` 从公开 Web/API 创建四个 Run，真实经过 pg-boss、Pi、OpenSandbox、queue 资料工具入口与 Artifact 保存。仅模型和搜索 HTTP 响应由 fixture 控制。主题任务 `d4be9638-595f-4594-bd0f-73ef1d0d49d8` / Run `7f384254-ee5c-49fe-adbc-4bc8e54bf9aa` 在 SearXNG fixture 阻塞时已有 `search_web` 的 `tool.started`，尚无 `tool.completed`；放行后返回标题、URL、摘要、空时间字段及 `duckduckgo CAPTCHA` 部分失败，随后读取 `https://example.com/` 的 HTTP 正文并保存报告。指定 URL 任务 `a1ed1721-911d-4e79-aa7d-324b2259f642` / Run `3f4b9058-3f67-4c6d-b2ae-a53631fc5810` 读取该网页并保存报告。元数据地址与控制面公网 IP 的工具调用分别被拒绝：任务 `7af13e7c-8ecd-42a7-8cfc-2157f107b7ab`、`a55086aa-edde-4131-8d69-f573aec8e064`。四个 Run 均为 `succeeded/cleaned`，OpenSandbox SDK 按 Run ID 查询无存活沙箱；负向任务的 fixture 仍提交固定报告，拒绝结论以工具事件为证据。

真实搜索另在独立 `19114` 测试栈验证。Web 只读挂载现有模型连接，queue 使用独立 `r108_live` / `r108_live_boss` schema，并接入现有私有 SearXNG 网络。`node infra/r1/test-live-research.mjs` 使用真实 sub2api `gpt-6-astra`：主题任务 `049b3cdd-57e3-4adb-8576-e7c3320735ee` / Run `c84c3669-55f7-42d9-a4d3-ec1c8e9d9c8e` 调用 `search_web`，获得 10 条结果，再读取 SearXNG 官方文档和 n8n 文档正文；报告分别标注搜索摘要与正文来源。本次 `unresponsiveEngines` 为空。指定 URL 任务 `66b0e133-e39e-47fb-940b-177ae4c2a1f4` / Run `5ea5283c-4816-460d-8624-a0a1198395a1` 首先读取 `https://example.com/`，得到 144 字符正文并在报告中引用。两份报告通过公开成果 API 可读，Run 均已回收。另从 queue 所在搜索网络查询中文词 `SearXNG 开源 搜索 引擎`：HTTP 200、39 条结果、本次无失败引擎。先前同服务同词查询的 37 条结果和 `duckduckgo CAPTCHA` 已记录于 [搜索服务部署说明](../../infra/r1/search/README.md)；引擎可达性会变化。

浏览器从真实工作页面验证主题与指定 URL 调研。[工具中追加截图](r1-08-steering.png)对应任务 `8b161816-ff7d-4689-9abb-629fd94e390a`：`search_web` 等待 fixture 时追加要求，放行后报告含引用与追加标记，刷新仍可读。[指定 URL 截图](r1-08-url.png)对应任务 `f9d3f2e2-cd00-47bc-8727-d59530bb6bb0`：先读取正文，再提交报告。fixture `/calls` 的安全摘录为四次模型请求：① `search_web`，无追加；② `open_public_page`，已有搜索摘要和追加标记；③ `submit_report`，已有搜索摘要、正文和追加标记；④ 最终回复。这个顺序证明追加在真实耗时工具结束后的下一模型请求生效，报告保留来源。完整用户文本和模型密钥未写入证据。

网页工具只处理可读取的 HTTP 文本；登录、非文本和无正文的 JavaScript 页面返回明确错误。报告中的事实陈述仍由 Agent 生成，本轮验证了来源数据、引用路径及持久化，未证明所有语义判断必然正确。正式公网入口未切换。

验收后清理了 `19114` 临时 live-test Web/queue、`r108_live` 与 `r108_live_boss` schema、live-artifacts 及临时 env/compose；确认数据库 active Run 为 0。正式 `data/model-connection.json` 仅作只读挂载，未修改或删除。

统一镜像 `r1-integration-369d91c` 又从公开 API 跑完五个隔离 Run。新增 Task `f082aedd-1f64-4dff-ae8a-6594916a1f21` 以 `https://httpbin.org/redirect-to?url=http%3A%2F%2F127.0.0.1%2F` 为初始公网地址，真实 HTTP 返回 302 后，`open_public_page` 的 `tool.completed` 为错误且包含“非公开地址”；随后固定 fixture 报告仍提交，拒绝结论只依据工具事件。五个 Run 均经 SDK 查询无活沙箱。

统一 `test-all isolated` 八脚本通过后，发现共享网络中的通用主机名 `queue` 可解析到多个服务实例。将隔离 queue 的 `TOOL_GATEWAY_ORIGIN` 配为唯一容器名并重建后，单独重跑 `test-research.mjs`：五个 Run 的主题、指定 URL、私网、控制面 IP 与公网 302 到私网场景均保持预期，报告持久化且 SDK 无活沙箱。
