# R3 双轴审查

基线：`55abef2`。两名 Sol 分别执行 Standards 与 Spec 审查，主会话复读实际调用链和写入位置，确认问题后安排修复。浏览器与正式发布结果另见 R3 整体验收记录。

## Standards

| 问题 | 处理 |
| --- | --- |
| 批注汇总、快捷命令可覆盖已有聊天草稿。 | `d95f19e` 在替换前确认；取消保留原草稿及待核对请求身份。 |
| 批注存储失败被吞掉，编辑表单仍清空。 | `d95f19e` 返回写入结果；失败保留编辑并提示，删除失败也保留批注。 |
| 部署文档缺少真实模型隔离栈的两个 schema。 | `2744d61` 明确四个 schema、所有者核验、env 映射及定向清理。 |

## Spec

| 问题 | 处理 |
| --- | --- |
| 回执恢复结果只挂原轮次，跨线程恢复缺少回执展示。 | `96202e5` 通过持久 resumes 关系投影原轮和恢复轮；保持同一业务操作身份。 |
| 正文或状态卡增高时可能漏跟随。 | `2964218`、`d95f19e` 将正文长度及回执、关联工作、状态卡变化计入滚动更新；上滚阅读保持原规则。 |
| 预览只加载一次，工作状态与新报告不可见；仅 URL 的工作标题为空。 | `2964218` 轮询真实状态并固定首次展示的报告版本，标题回退到来源 URL。 |
| 键盘无法触发批注，正在输入的意见无法跨刷新/版本恢复。 | `d95f19e` 按 Task 与不可变报告版本暂存编辑；`07c8eaa` 将键盘扩选与 Enter 进入批注分开。 |

主会话另确认版本预检存在异步覆盖新输入的风险，`d95f19e` 仅附加原提交身份并保留最新草稿。新选区替换未保存意见也经过确认，小屏汇总成功后返回聊天框。

快捷命令只携带 Task 时，草稿停留期间可能被新问题或新 Run 替代。`7405e17` 将 Interaction/Run 身份写入快捷命令，服务端核对原文并在既有冻结事务内校验目标；旧手输命令继续兼容。真实 PostgreSQL 聚焦回归验证旧问题保持隔离、旧 Run 控制/重试被拒绝，以及新语法接受后的回执恢复。主会话复核了解析、请求哈希、冻结锁行和恢复调用链。

Standards 三项、Spec 五组问题均已有对应修复；主会话追加发现的异步覆盖与键盘选区焦点问题也已修复。最终候选还须通过浏览器及统一发布验收。
