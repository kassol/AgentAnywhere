# R1-07 报告成果隔离回归

日期：2026-09-18。目标：cc-la 的 `agentanywhere-r1-test` 隔离栈；正式 R1、W0、SearXNG 与 PostgreSQL 实例保持原状。Web、queue、Agent 和模型 fixture 使用独立 `r1-07` 镜像。模型响应由 `model-fixture.mjs` 控制；PostgreSQL、pg-boss、Pi 0.85.1 和 OpenSandbox SDK 0.1.11 均运行真实组件。

执行 `node infra/r1/test-run.mjs` 两轮。每轮从公开 API 创建 5 个成功 Run、2 个协议失败 Run 和 1 个保存失败后重试的 Run。成功 Run 均先执行 echo，再调用 `submit_report` 写 Markdown 与文本附件。API 下载的 12 个成果版本逐个与数据库登记的 SHA-256 一致；销毁真实沙箱并重启隔离 Web 后重新登录下载，SHA-256 保持一致。匿名成果请求返回 401。5 个成功 Run 均为 `succeeded/cleaned`，2 个协议故障为 `failed/cleaned`，SDK 按 Run ID 查询均无存活沙箱。

保存失败场景临时将隔离 `test-artifacts/` 设为不可写。Run 进入 `save_failed/blocked`，无 Artifact/Version；SDK 查到沙箱仍存在且 `expiresAt=null`，即手动回收生命周期。恢复目录权限并经公开 API 请求重试后，报告和附件均保存，Run 进入 `succeeded/cleaned`，沙箱删除。测试脚本的 `finally` 恢复目录权限；结束时远端目录为 `700 1000:1000`，隔离 Web 健康。

本轮只证明 fixture 给定内容在真实链路中的复制、校验、保存、访问及回收。报告事实正确性和真实搜索来源由后续调研链路验收；浏览器安全与下载验证见下文。

主线程使用 ego-browser 在同一隔离 Web 阅读已完成报告并下载 `report.md` 与 `notes.txt`。报告区域没有 script 节点，`window.reportXss` 未被设置；HTTPS 来源链接保留 `noopener noreferrer`，恶意 `javascript:` 链接的 href 被清空。下载的 SHA-256 分别为 `817cf4a61cf3d1dd238f06737ff9e0874a261bf431c77072acfda8b9dc7cd691`、`fa7b57ddeea09e347209cca4c51ebb77b9eca97117e1d4fea3126802dee7cc30`，与公开 Task API 的成果记录一致。[浏览器截图](r1-07-artifacts.png)。

补充运行 `node infra/r1/test-report-contract.mjs`。模型 HTTP fixture 分别提交 0 字节附件，以及与报告同名的 `report.md` 附件。两项工作均通过公开 API 进入 `succeeded/cleaned`。前者的附件 API 下载为 0 字节，SHA-256 等于空文件哈希；后者先收到 `submit_report` 工具错误“附件名称重复”，修正附件名后再次提交成功，并形成两个成果版本记录。该回归贯穿真实 queue、Pi 与 OpenSandbox。
