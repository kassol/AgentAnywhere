# R4-06 报告审阅与改稿迁移证据

日期：2026-09-20
Issue：[#48](https://github.com/kassol/AgentAnywhere/issues/48)
固定上游：Craft Agents OSS `v0.13.3` / `e8963854c3679edcceb105a42537a06749e6cb64`

## 实现

- `src/web/craft/components/AnnotatableMarkdownDocument.tsx:248-354,640-689` 保留上游 selection/controller、跨文本节点偏移、覆盖层几何、批注岛、编辑和删除结构；安全 Markdown 由调用方注入。恢复草稿按真实 DOM Range 定位；菜单与 blocker 同在 body portal，菜单层级更高。`WorkPreview.tsx:441-493` 把它接到真实 Task/Version 报告。
- `ReviewAnnotations.ts:119-166` 在本地 `ReportAnnotation` 与上游 `AnnotationV1` 之间适配，Task/Version、position+quote、意见和时间不丢失。`annotation-core.ts:72-111` 用 UUID 建立可持久化的新批注身份。`WorkPreview.tsx:319-384` 延续按版本 localStorage 草稿、失败保留和精确写入。
- `selection-persistence.ts:1-45` 在写批注前先把稳定 UUID 写入草稿；草稿身份写入失败时不写批注。批注或草稿清理失败时编辑器保持打开；再次保存按同一 UUID upsert，避免重复新增。`ReviewAnnotations.ts:202-219` 仅在已接受批注和匹配草稿都成功清理后通知界面；失败时已接受消息不会重发，批注仍可见并提示手动清理。
- `WorkPreview.tsx:404-405` 仅把正文仍精确匹配的批注送入覆盖层，失效引用保留在未发送列表。`Textarea.tsx:10-19` 用 React 18 `forwardRef` 保证键盘选区读取原生 selectionStart/selectionEnd。
- `DocumentFormattedMarkdownOverlay.tsx:57-90` 保留上游报告卡结构，并移除只适用于全屏父容器的最小高度与垂直居中，使它适配 A 右侧普通流预览。
- `annotation-resolver.ts:64-182` 对上游“合法 position 优先”作安全收紧：position 必须同时匹配 quote；无上下文的重复 quote 不猜测位置。`AnnotationIslandMenu.tsx:74-100` 用原 activeViewSize 将展开编辑器限制在视口内。
- `Island.tsx:554-574`、`IslandFollowUpContentView.tsx:117-154` 忽略 IME composition、keyCode 229 和已处理 Escape，避免取消候选时关闭批注或报告。
- `craft-theme.css:41-46,88-93` 与 `craft/styles.css:53-75` 迁入上游 `info-rgb`、阴影透明度和 `shadow-tinted`，保留批注高亮及编号 chip 的明暗主题阴影。

上游 block shift-click 依赖 Craft Markdown 的 `data-ca-block-*`，本地安全 renderer 不提供该契约，因此删除该分支。上游“保存并发送”直接进入 Craft 会话发送器；本产品经 reviewBridge 汇总后由现有 Composer 提交，因此裁掉该分支。中文文本替代上游 i18n。

## 自动验证

```text
$ bun test src/web/ReviewAnnotations.test.ts src/web/WorkPreview.test.tsx src/web/craft/components/annotations/annotation-resolver.test.ts src/web/craft/components/annotations/selection-persistence.test.ts src/web/craft/components/Textarea.test.ts
25 pass
0 fail
70 expect() calls

$ bun run typecheck
$ tsc --noEmit

```

回归覆盖版本隔离、真实创建至 reviewContext 的 UUID 持久化链路、旧草稿恢复、身份写入失败阻断、批注写入及草稿清理失败后同 ID 重试、接受清理失败、React 18 textarea ref、精确清理、未知版本、合法偏移但原文变化和重复原文歧义。跨 DOM 节点选择、首段/小屏几何、刷新恢复、批注汇总与新版确认由主线程执行真实浏览器验收。

## ego-browser 真实验收（2026-09-20）

同一 TaskSpace 46，隔离 Web 19114 与固定前端 5174；真实模型 gpt-6-astra。跨标题和段落鼠标选择、键盘全选、批注保存/刷新恢复/编辑/删除、小屏返回对话均通过。浏览器验证发现并修复 portal 遮挡、非 UUID 批注刷新丢失、React 18 Textarea ref 与嵌入报告布局问题。

- Task `e3b6c946-5a32-4fbb-a9b8-0d41133bab54`，首版 `f3a31dfe-4076-41a6-b58a-3bb9fc6bebff`。
- 汇总批注发送后，新 Run `9a776685-7eeb-40b7-b737-f0008d82fc7d` 成功，生成版本 `ef2a40c3-0bbb-4e23-9792-1cce527b4073`；已接受批注和提交草稿清除。
- 再基于首版改稿，新版冲突取消保留草稿且管家轮次数保持 6；确认继续后轮次数为 7。Run `de8c1724-5b0b-4581-8aac-012f197737d7` 成功且 `previousReportVersionId` 仍为首版，生成版本 `647f6d58-3570-4515-8444-48c1a5d6452e`。
- 首版改稿后实际浏览器下载 SHA-256 仍为 `ea488b7d4fb17048c48c32d3cf7e9f109923d9a299937e8a4df1e8269a4a729e`。
- 仅对隔离页面当前批注草稿键注入 `Storage.removeItem` 失败：界面显示清理失败，保留草稿与已保存批注。刷新恢复原生 Storage 后重试，UUID 仍为 `11d544ff-f6c4-4eae-80fd-5ff790dc93a0`，批注数量为 1，`createdAt=1789881079787` 保持，`updatedAt` 更新，草稿清除。最后通过 UI 删除测试批注。
- 小屏 390×844 的 Island 保存按钮可点击、无横向页面溢出；截图 `report-mobile-island.png` 在本轮发布证据中归档。

IME 验证使用 Chromium 输入法组合事件；未模拟操作系统候选词窗口。
