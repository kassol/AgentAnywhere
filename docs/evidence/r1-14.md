# R1-14 发布准备证据

状态：完成部署核查、本地发布工件、私有第一阶段备份与 TLS 私钥权限修正；正式部署、公网切换、浏览器与完整闭环验收尚未执行。本文件不得作为 Spec #3 T01—T17 的通过证据。

2026-09-18 在指定主机 `cc-la` 只读核查：`docker ps` 显示 W0 Craft 在 `127.0.0.1:19100`，正式 R1 Web 在 `127.0.0.1:19110`，隔离测试 Web/fixture 在 `19112/19113`。`curl` 得到公网根路径 HTTP 302、R1 本机 `/login` HTTP 200。`/opt/1panel/www/conf.d/agent.riverflows.in.conf:33` 引入站点代理文件，`/opt/1panel/www/sites/agent.riverflows.in/proxy/root.conf:2` 当前转发到 W0 的 `19100`；OpenResty 当前配置语法检查通过。未读取或输出密码、密钥内容。

现役正式 `/opt/agentanywhere-r1/compose.yaml:4,36,41` 使用三个 `r1-05` 镜像，Web 没有 `artifacts/` 挂载，queue 没有成果挂载或搜索网络。仓库发布 Compose 在 `infra/r1/compose.yaml:4,24,39,44,55,63` 加入这三项并将镜像统一为完整发布 SHA；`infra/r1/test.compose.yaml:4,23,29,38` 用同一测试 SHA 选择四镜像。`infra/r1/deploy.sh:4-33` 在重建容器前校验 SHA、三镜像、PostgreSQL 健康和 Compose 配置；部署脚本不负责构建、备份或公网代理切换。正式发布和回退的可执行步骤见 `infra/r1/README.md`。

数据边界经 `docker inspect` 与主机文件权限核对：W0 私有状态在 `agentanywhere-w0_craft-data` 卷，密码文件 `/opt/agentanywhere-w0/craft.env` 与 `webui-password` 均为 0600；R1 正式数据库卷为 `agentanywhere-r1-postgres-data`，模型连接在 `/opt/agentanywhere-r1/data`。站点 TLS 私钥初始为 0644，`/opt` 至站点 `ssl/` 的父目录均为 0755；root 已改为 0600，OpenResty master/worker 当前以 root 运行，修改后 `openresty -t` 实际通过，未 reload。

私有发布备份 `/opt/agentanywhere-backups/20260918-r1-release` 为 root:root 0700：R1 数据库 dump、正式 `data/`、现役 Compose/deploy/env/密码、W0 配置密码与容器 inspect、旧代理文件均已保存；`pg_restore --list` 实际通过，备份时正式 `artifacts/` 尚不存在。Craft 上游归档内 `packages/shared/src/credentials/backends/secure-storage.ts:84-98,336-343` 指向容器内 machine-id 或 username/home，旧版还用 hostname。容器内 Node `existsSync` 已确认两个 machine-id 路径均不存在；实际身份元数据已保存到该备份的 `w0/identity.json`（0600）。W0 用户数据卷与最终工作目录仍须在公网切换稳定后停机归档并校验；正式 R1 的数据、Sandbox 与搜索服务保留。

尚需记录：固定发布 SHA 与镜像 ID、W0 最终用户数据归档及校验结果、切换前后代理状态、全部 T01—T17 的真实 API/浏览器证据、测试资源回收及 W0 最终回收结果。凭据内容不得公开。

19114 真实网关隔离栈先完成不启动的准备：`/opt/agentanywhere-r1/live-test.prepared.compose.yaml` 使用完整 `LIVE_RELEASE` SHA，Web 在 `127.0.0.1:19114` 且通过 `live-test-web` 别名供 queue 访问；queue 加入专用 SearXNG 网络。`live-test.env` 为 0600，PostgreSQL 应用/队列 schema 分别为 `r114_live`、`r114_live_boss`，已创建并核对；`live-data/` 与 `live-artifacts/` 为 0700、1000:1000，模型连接从正式文件字节一致地复制到独立 `live-data/model-connection.json`（0600、1000:1000），Web 挂载可写私有副本。占位完整 SHA 的 Compose 配置解析通过。准备阶段没有创建 live 容器或 Run；19112/19113 隔离栈保持原状。

随后使用发布 SHA `f9a0f7b5ec29b6f8fbb40463de26e69e6e239fa8` 启动该 Compose 的 Web 与 queue，未执行 live 测试脚本。Web 为 running/healthy，queue 为 running；本机 `19114/login` HTTP 200、匿名 `/api/tasks` HTTP 401、测试密码登录 HTTP 204 并设置 session Cookie。Web `/data` 是可写的 `live-data/` 目录挂载，`/artifacts` 只读；queue 的 `live-artifacts/` 可写且接入 `agentanywhere-r1-search`。正式与 live 模型文件 SHA-256 均为 `9f8abc66eb58057c9abfec881113457546978e44fd2acf89d79a4ed249708c5d`，正式文件在启动前后未改变，两文件 inode 不同。只读公开 API 已确认私有副本含 `gpt-6-astra`；`r114_live.work_runs` 计数为 0。19112/19113 未修改。
