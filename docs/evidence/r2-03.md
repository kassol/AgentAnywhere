# R2-03 查询历史工作与解读成果

对应 [Issue #21](https://github.com/kassol/AgentAnywhere/issues/21)。查询候选不会建立关联；明确读取后，同一工作可关联多段管家对话。报告引用包含固定版本，读取与比较不新增 Run 或成果。

- 本机公开 API 与真实 PostgreSQL：`src/steward.test.ts` 5 项测试、59 个断言通过。新增回归覆盖候选浏览、显式关联、浏览权限升级、旧候选直接冻结、分页尾页伪唯一、比较缺版本与回答跳过读取。类型检查、目标 Work 回归与差异检查通过。
- 授权：当前用户消息和结构化回执用于隔离规划；新目标需要当前消息原文的唯一查询结果。读取范围持久冻结，回答工具只接受固定操作回执。报告内容无法增加目标或工作操作。
- 资源与成果：复用报告路径、大小、哈希和 UTF-8 校验；附件正文在读取前拒绝。Web 停止等待请求结束后关闭业务数据库，管家轮次始终清理计时器。
- cc-la 独立 `19112` 栈运行 `f1776a109d63b048bd11a7482af89db3a3f093ca`。`infra/r1/test-steward-query.mjs` 通过真实 Web/API、PostgreSQL、队列、Pi 和 OpenSandbox 生成两项报告及其中一项的第二版；模型 HTTP 可控。双协议读取、同一工作关联两段对话、跨工作比较、同工作两版比较、含糊目标澄清、8 次实际模型请求硬上限均通过。报告注入未进入规划器；错误操作回执没有扩大读取范围。读取期间工作数、Run 与成果版本均保持不变。
- 报告 Task：`90d69378-683b-4728-8cf6-ad1723290fb2`、`63553763-4e70-4d3d-9101-c0e6a79affb5`。同工作两版比较对话：`b3a3b949-f32e-48db-99a0-cb6c40c546b2`。本票验收没有修改正式站点数据或配置。
- ego-browser TaskSpace 32：键盘提交中文解读请求，完成后刷新仍保留回复与关联工作；从关联卡打开旧版成果，下载链接准确指向该版本。报告中的脚本未运行，页面没有 JavaScript 协议链接。[截图](r2-03-query.png)显示持久回复和真实关联工作，中文控件无溢出。

上述隔离测试的模型工具响应使用可控 HTTP，真实模型补验另见下节。统一全套及正式发布在 R2-12 完成。

## 真实模型补验与运行时修复

真实 Chat Completions 调研已生成报告并回收沙箱：Task `3d3cfcdb-db45-47f1-afc8-54bff276d349`、Run `68fa160c-b6f3-4523-957d-a58135bcd33f`、成果版本 `0ed9f7e6-e1b4-4588-8b21-1230d88b2b48`。随后 Responses 管家对话 `ba59dc44-f089-4097-bef9-663271b21828` 已读取报告并开始输出解释，但数据库事务停滞，轮次未结束；因此本票重新打开，不能将本次读取记为通过。

部署镜像运行 Bun 1.3.10，本机回归运行 1.4.2。现场连接处于 `idle in transaction`，无 PostgreSQL 锁阻塞，事务中出现普通事件查询；逐项检查应用的事务回调，未发现调用非事务连接或自嵌套事务。这与 Bun 官方已确认的[并发连接池缺陷 #32004](https://github.com/oven-sh/bun/issues/32004)一致。[修复 #34756](https://github.com/oven-sh/bun/pull/34756)和[修复 #35114](https://github.com/oven-sh/bun/pull/35114)均为 `bun-v1.4.2` 的祖先提交，GitHub compare 的 `behind_by` 均为 0。

同一 PostgreSQL、同一 `max=1` 并发负载对照：1.3.10 在 8 秒超时，流式事务仅完成 80/240、事件查询 12/1200、heartbeat 0/120；1.4.2 在 1087 毫秒完成全部 240 个写事务、480 次详情事务、1200 次事件查询和 120 次 heartbeat。对照只使用临时 schema，均已清理。Web 构建和运行镜像固定为 `oven/bun:1.4.2-slim@sha256:cb3bbbb08e13a4a2ff400f24c7a2a1d5efa83f6ef8544d52d95a519631e2fc61`。

复现脚本：`DATABASE_URL=独立测试库 bun infra/r1/check-sql-concurrency.ts max1 r2_bun_sql_diag_check`。旧运行时超时退出后，用同一独立测试库执行 `bun infra/r1/check-sql-concurrency.ts cleanup r2_bun_sql_diag_check` 清理；禁止使用正式数据库。

`91369ea91192d6ded36356b9a7b1400ad90e7f04` 已部署至两个测试栈，容器内 Bun 1.4.2；定向模型配置、工作和管家公开 API 回归 10 项、290 断言通过。旧轮次按中断恢复，正式五个核心容器基线未变。

ego-browser 在独立 live 栈使用真实 `gpt-6-astra` 完成两种协议的报告工具往返：

- Responses：原对话中明确继续，轮次 `ae68586a-6591-481c-b574-293a818f5111` 完成，3 次实际请求、14076 毫秒。
- Chat Completions：新对话 `70f0c1f3-2ff9-433e-9cd7-065f0b560cf0` 从明确工作 ID 查询并读取，轮次 `4a6247ce-eeda-4b7a-91da-84b5265e662b` 完成，4 次实际请求、16989 毫秒。

两次均给出正确报告链接；刷新后回复保留。工作、Run、成果版本仍各 1，沙箱已回收。[真实模型截图](r2-03-live.png)显示解读与原报告关联。补验通过后重新关闭本票。

## Standards

固定基线 `00cb588`。独立评审发现分页唯一性、异常计时器清理、停止顺序和附件读取顺序问题，均已修复。

## Spec

独立评审发现候选越权、比较版本缺失、未读却完成和同工作多版本比较限制，均已修复。公开回归补齐旧候选与分页边界，隔离端到端补齐真实两版报告比较。
