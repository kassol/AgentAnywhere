# R2-06：管家回答 Interaction

实现 `b53eb2ef64d27a4dc323951560f49e48b023a73e`；Issue #24。

普通回答仅接收完整明确的“回答：<原文>”或“回答工作 <TaskUUID>：<原文>”；额度决定使用完整“继续/结束”或“继续工作/结束工作 <TaskUUID>”。服务端冻结同一 Task、Run、epoch 和 Interaction，直接详情 API 与管家共用首次回答事务；关联、恢复入队与操作回执同时提交。否定或引用中的子串不能形成回答授权。

## 验证

- 独立复核发现并修正同答案在 epoch 推进后误报过期、冻结与回答锁序不一致、普通答案可从否定或引用中截取三项问题；后续调用链复核通过。类型检查、构建通过；目标 Work/Steward 回归 9 项、217 断言通过，随后追加恶意模型普通回答回归 6 断言通过。
- cc-la 隔离运行 `node infra/r1/test-steward-interaction.mjs` 通过：真实 PostgreSQL、队列、Pi 和 OpenSandbox，仅外部模型 HTTP 受控。两种管家协议均回答成功，同一 Run 恢复并完成报告。
- Chat 对话 `b3dc48b8-2fed-417e-b422-b49b4188830f` 的回执 `7b221e39-a28b-479c-9891-e3a4b89ce8cb` 在业务提交后故意挂起模型响应，SIGKILL/start Web 后显式继续仍返回旧回执；原 Task 新 Run 的新问题保持未回答。
- Responses 对话 `c7ebde0b-151e-4880-9590-25e0124285ec` 的回执 `f28e0c08-f7a7-4962-91c8-ac553aad7a6a` 完成真实恢复。
- 额度 Task `5c3187a3-e496-4cae-8ec1-1848a6e895be` 明确继续后增加 40 次额度并完成；另一个额度问题明确结束后取消。否定句没有回答或续额。
- 竞争回答 Task `c48a2497-054f-4681-8fb5-e26044c79d00` 两个不同答案并发仅一个 202、另一个 409，只有一个 Run。对话 `8282a931-6e75-4af6-836a-09922f9b2bbe` 在模型 HTTP 暂停期间由详情先回答，等待 Run epoch 提升并完成后释放管家工具调用，同答案仍取得 accepted 回执，没有再次入队。
- 全部隔离测试相关 Sandbox 已回收。部署测试版本前后正式五个服务 ID、启动时间和重启次数保持不变。
- ego-browser TaskSpace 32：中文键盘提交“回答工作 54c9daa7-6e82-4136-bd23-72c9de717fde：机制方向”，管家对话 `7672030a-1a83-49c4-bfbc-95bc33feccf7` 显示真实回答回执、待办归零及完成成果 `259d811d-2fc1-485b-9d14-1996b6de480e`。截图 `r2-06-answer.png` 已检查。

真实网关全量 R2 回归与正式部署由 #30 汇总。
