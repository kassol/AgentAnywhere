# R4 发布复核

固定起点：`c5ad1f2b3a00ef778aef60c7af38d9b48dee496b`。两个 Sol 复核分别覆盖项目规范与 Spec/上游源码；主线程核对实际调用链与持久化位置。

已修复：原生控件残留、合法模型 ID 与未选择哨兵冲突、选择模型触发的请求身份更新、全局 CSS 覆盖 Craft 字号/字体、批注 UUID 与 React 18 ref 兼容、批注 portal 遮挡、存储失败重试和接受清理语义。来源表已按实际运行时 import 链整理，复制文件记录固定版本、原路径、许可及本地修改，Web 镜像附带许可与 NOTICE。

25 项批注/存储/控件聚焦回归（70 assertions）、typecheck、构建通过。`1e92847` 的完整业务回归为 81 项 Bun 测试/624 断言、16 项 isolated 与 3 项 live 全通过。最终 `0fc0367` 仅在 `Work.tsx:271` 修正徽标网格拉伸，并在 `craft/styles.css:36-49` 按 slot 补上游 Preflight 控件重置，未改变业务接口。源码静态设计扫描输出为空；保留用户已确认的 Inter，未增加设计忽略。

浏览器证据见 R4-04 至 R4-08。隔离环境已覆盖鼠标/键盘引用、IME、发送幂等、停止、待办、固定版本改稿、冲突、存储故障恢复与小屏；正式环境已覆盖旧数据/哈希/草稿、键盘主流程、Select/Switch，以及五页浅色、深色、小屏共 15 张矩阵。`0fc03673e44a89b8c9b188575c4d3fceba3d9596` 已由项目部署流程发布，配对备份、public 检查和定向清理通过；完整身份与结果见 `r4-08.md`。
