# R4-07 模型设置迁移证据

日期：2026-09-20  
Issue：[#49](https://github.com/kassol/AgentAnywhere/issues/49)  
固定上游：Craft Agents OSS `v0.13.3` / `e8963854c3679edcceb105a42537a06749e6cb64`

## 实现与业务边界

- `src/web/craft/components/SettingsSection.tsx:1-39`、`SettingsCard.tsx:1-24`、`SettingsRow.tsx:1-22` 和 `SettingsInput.tsx:1-50`、`SettingsSelect.tsx:1-34`、`SettingsToggle.tsx:1-38` 保留固定上游设置组件的结构；底层 Input、Label、Select、Switch、Badge 位于同目录并由运行页面直接 import。Select 和 Switch 触发器通过 `aria-labelledby` 和 `aria-describedby` 关联可见标签与说明。
- `src/web/ModelSettings.tsx:131-209` 使用这些组件呈现日常模型、人工调研模型池、连接、发现目录和逐模型能力。网关发现只提供“选择”动作，调研池只由 `:146-155` 的明确 Switch 操作修改。
- `src/web/ModelSettings.tsx:20-42` 保留完整模型保存 payload 和刷新期间未编辑字段保护；UI 保留值长度为 201，超过服务端模型 ID 上限，因此合法 ID `__unset__` 仍可选且完整保存。`:65-113` 保留原 API、失败消息与成功后清理时点。连接密钥初始值始终为空，`:161-167` 只显示服务端 `hasCredential` 状态，保存成功后仍清空本地密钥。
- `src/web/main.tsx:44-53,182-188` 用 SettingsSection/Card/Row 组合主题与账户；`:56-95` 用 SettingsInput 内的 Craft Input/Label 和 Craft Button 保留原登录请求、错误状态和失败后输入值。
- `src/web/style.css:15-19,29-32` 只让旧原生控件规则避开 `data-slot` 组件；`:64-70` 禁止设置页的直接 flex 子项收缩，避免固定视口高度下的卡片裁切；`src/web/model-settings.css:1-207` 清理旧设置实现后保留详情折叠、桌面双列和 680px 单列交互。未修改 `theme.ts`、服务端 API、持久化对象或 Run 快照。

## 自动验证

```text
$ bun run typecheck
$ tsc --noEmit

$ bun test src/web/ModelSettings.test.ts src/web/craft/components/SettingsSelect.test.ts src/web/craft/components/SettingsToggle.test.ts
5 pass
0 fail
17 expect() calls

$ bun run build
✓ 2511 modules transformed
✓ built in 1.85s

$ impeccable detect --json <R4-07 changed UI files>
[]
```

`ModelSettings.test.ts:26-79` 覆盖完整保存协议、目录映射、人工覆盖值、管家模型和人工池，以及刷新失败/成功时未保存选择的保留与替换；同名 `__unset__` 模型选择与 payload 也已回归。`SettingsSelect.test.ts:6-22` 和 `SettingsToggle.test.ts:6-22` 覆盖 combobox/switch 的可见标签与说明关联。按任务约束未启动服务、未执行浏览器验收；桌面、小屏、浅深主题、键盘、保存刷新和错误恢复由主线程统一验收。


## 浏览器验证

ego-browser TaskSpace 46 在隔离真实网关栈验证：管家协议从 Chat Completions 保存为 Responses 后刷新读取一致，再恢复原协议；模型池、已选模型与凭证保留。密钥留空保存连接后输入仍空且服务端 `hasCredential=true`。断网保存显示失败且保留未保存池选择，恢复网络后重试成功并恢复原配置。折叠详情保持字段。

390×844 视口初次发现父 Panel 的 flex shrink 裁切设置卡片；设置页直接子项设为不收缩后，三个设置区的 clientHeight 与 scrollHeight 一致，页面宽度和 scrollWidth 均为 390。主题三项完整可见，管家模型与协议选择器均关联可访问名称。截图保存在本轮私有验收目录，最终发布证据统一归档。

登录错误时显示“密码错误，请重试。”并保留输入；使用隔离密码重新登录成功。正式登录态及模型配置未更改。

最终 Switch 原组件通过真实点击与保存：移出人工池后 API 返回 `[]`，重新开启并保存后返回 `["gpt-6-astra"]`。连接和凭据保持原值。实际按钮计算字体为 Inter 与既定中文回退字体。
