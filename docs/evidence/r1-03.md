# R1-03 模型元信息验收记录

- 2026-09-18 核查 `https://models.dev/api.json`：Bun `fetch` 返回 HTTP 200、`application/json`。`openai.models["gpt-6-astra"]` 包含 `limit.context=1050000`、`limit.output=128000`、`modalities.input=[text,image,pdf]`、`reasoning=true`、`tool_call=true`、`cost.input=10`、`cost.output=50`，并包含阶梯价格。目录无协议字段。数据只作参考，不表示网关调用或能力验证。
- 上游来源：[models.dev API](https://models.dev/api.json)；[原仓库](https://github.com/anomalyco/models.dev) 的 `LICENSE` 为 MIT，Copyright (c) 2025 models.dev。本工程从线上 API 获取目录，没有复制上游代码或目录快照。`models.dev` 的价格是基础参考价；阶梯价格、实际用量和网关计费另行区分。
- 网关已核查的 sub2api `/v1/models` 响应只提供 `id`、`display_name`、`owned_by`、`type`、`created`，无模型能力或价格。实际目录匹配只在当前网关成功刷新后使用完整 `owned_by/id`；别名由所有者显式指定 `supplier/model`。字段优先级为人工覆盖、当前网关元信息、明确匹配目录、未知。
- `bun run build && bun run typecheck && bun test` 通过。测试从公开登录和设置 API 保存、读取并重启；只在外部网关与 models.dev HTTP 边界提供可控响应。验证了逐字段来源、人工覆盖撤销、别名、近似名称保持未知、目录失败保留缓存和凭证变更后不复用旧网关身份。
- 页面端到端交互与正式 Run 费用/模型调用由后续集成验收；本票的目录同步不进行模型调用，也不证明协议可用。
