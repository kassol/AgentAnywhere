# R5-09：真实执行、页面体验与发布验收

状态：实现及正式发布完成；设置保存失败的本轮浏览器补验待完成。正式代码版本 `0d0a79849458b4246bd597bb6a28bf1fe25beda2`，公网 https://agent.riverflows.in。

## 实现与复核

- 起点 `858b451`；页面基准为 A `412e7d0`，固定 Craft v0.13.3。
- Sol 实现与主线程集成覆盖 #52–#59。独立 Standards / Spec 复核及修复见 [R5 复核](r5-review.md)。真实组件与业务组合边界见 [来源清单](../r5-craft-source.md)。
- 本地全量：84 tests、667 assertions 通过。后续人工标题边界补充先失败再通过；报告来源与链接按钮修复后，59 项前端检查、173 assertions、typecheck 和 build 通过。
- 构建仍提示单个主 JS chunk 超过 500 kB；本轮未引入新的运行时依赖。

## 已完成的浏览器检查

使用 ego-browser TaskSpace 72，同一 p1，1440×1000 与 390×844。以下本机检查使用真实 Web/API/PostgreSQL；模型 HTTP 为受控 fixture，不据此声称真实模型已验收。

- 从登录页输入错误密码，错误可见；正确密码登录；设置页退出回登录。
- 对话改名、保存、刷新持久化；列表搜索无结果、清空恢复；移动导航与对话入口可达。
- 从完整创建表单建立工作，手动命名“同名工作”，刷新后标题保持，原始目标完整；排队工作取消后显示真实“已取消”。
- 中文组合输入时 Enter 不发送；Shift+Enter 换行经刷新与提交保留。
- 断网发送保留内容及原请求身份；刷新后通过“核对并重试”成功，只增加一轮。800ms 网络延迟下提交期间输入新草稿，旧响应未清除新内容。
- 日常设置中移出模型池、保存、刷新确认，再加入并保存；配置状态显示真实成功反馈。
- 浅色、深色及跟随系统的两种媒体值均验证；小屏设置导航宽度为 390px，无横向溢出。
- 独立报告路径由匿名重定向和认证 HTML 路由回归覆盖；小屏报告索引可展开主导航。

## 隔离执行与浏览器业务流程

cc-la 使用独立 schema `r1test_c13ac03c0efd` 和队列 schema，通过 SSH 隧道访问 29112。模型 HTTP 使用 fixture，队列、PostgreSQL、OpenSandbox 和成果存储均实际运行。

- 完整 isolated 在 `c13ac03` 通过：84 tests / 667 assertions，以及 16 组集成检查。覆盖双协议、报告附件、搜索边界、追加要求、回答 epoch、取消、继续工作、失败恢复、40 次调用上限、历史查询、多工作派发、精确控制、全局待办、摘要、重试和改稿。
- 最终修复的标题、报告来源、输入样式候选 `e699fc5` 在 Linux 容器补跑 64 tests / 221 assertions，全部通过。`b380a89` 随后让列表沿用既有轮询，`2eaf405` 修正小屏工作/待办页头；两项另由构建和浏览器验证。
- 浏览器 Thread `8f75ec90-2782-4f08-b4e6-d65db2addcea` 派发两项独立工作 `f7ffbf24-c784-4804-83ba-61ca521843a3`、`89b392f1-e7a1-4a64-86a2-1e1a689c3c70`；完成后每项仍只有一个集中入口。初次含泛化“委托工作”的 fixture 输入未获得调研操作，使用明确“分别调研”后才派发；该受控夹具不用于判断真实模型自然语言能力。
- 从完整表单直接创建 `77d811e2-36f0-4f1d-b0d5-71f7f4eeed0b`，经真实执行产生 Interaction；在 `/pending` 回答，条目消失，原 Run 恢复并成功交付。
- 在失败工作 `b43de580-44d4-40a1-acf1-2c8507290398` 点击手动重试，新 Run 引用 `339b3844-b2b8-451f-8472-f339b160ff08`，最终成功；原失败记录保留。
- 从对话集中入口打开报告，再切独立阅读；键盘选择原文、保存批注、刷新恢复。汇总回同一 Thread 的 Composer，内容包含真实 Task/Version 与引用，点击发送后才创建改稿 Run。
- 新版 `1776fa62-6c29-4b3b-9951-021c83c892b2` 含批注标记 `CONTINUATION_MARKER_16`；旧版 `4d8668f6-2350-4f54-bcf3-b3ac4802043e` 前后下载哈希均为 `817cf4a61cf3d1dd238f06737ff9e0874a261bf431c77072acfda8b9dc7cd691`。新版哈希 `9f118a6f650b8d25ea16e5893bab91257789aee61943305de6cba2f3c36a9359`。
- 小屏切回旧版下载，再返回原对话；来源 URL 保留。全部模型任务结束后，隔离队列停止，切入真实网关验收。

## A 视觉对照

六类页面的浅深色、1440×1000 与 390×844 截图在 [r5-ui](r5-ui/)。登录使用本机真实服务；设置、工作、待办、对话、报告矩阵均使用正式公网数据。截图均由 ego-browser 生成并人工查看；fixture 的重复“自动标题”来自受控模型返回，不是页面占位数据。

| 页面 | 与 A 一致部分 | 保留的产品差异与理由 |
| --- | --- | --- |
| 对话 | 190px 主导航、240px 会话列、窄页头、760px 正文、用户气泡、连续助手正文、底部 Composer | 保留持久轮次标识与精确命令身份；工作历史默认折叠，避免失去恢复证据。 |
| 工作 | 独立工作入口、800px 内容、分隔线列表、短标题与状态 | 每项保留原始要求展开和真实操作入口，行高高于原型；完整创建表单收起后可展开。 |
| 待办 | 窄内容区、Craft PermissionRequest、等待提示与动作 | 展示真实 Task/Run/Interaction 身份，问题回答使用文本框；原型的固定答案不代表实际提问。 |
| 报告 | 独立主阅读区、明确返回、版本切换、下载和批注 | 保留附件、固定版本和键盘引用选择；无报告时展示真实索引空态。 |
| 设置 | 三栏外壳、分组行、开关与轻边界 | 保留完整协议、映射和能力字段，低频配置折叠；主题提供三种明确选择。 |
| 登录 | 克制品牌、单密码入口、无装饰大卡片 | 密码显示切换、限流和请求错误由真实认证状态驱动。 |

视觉复核修正了深色 Input/Textarea 的浏览器默认白底黑字、按钮链接继承色、设置会话列和小屏工作/待办页头。保留用户确认的 Inter 与既有窄范围忽略，无新增忽略。

## 真实网关与升级证据

- `b380a89` 的 live 套件完整通过：Chat Completions / Responses 工具执行、流式增量、取消、无效模型 404、持久化与清理；真实搜索返回 10 条结果（1 个搜索引擎失败），公开页读取 144 字符。管家真实派发、提问回答、追加要求、报告解读、改稿和取消全链通过，旧报告哈希保持。
- 正式升级前核对原有 4 个工作、3 个对话、5 份报告，升级后旧记录、报告哈希、模型配置保持，旧草稿兼容检查后恢复为空。
- `2eaf405` 发布前建立 `20260920-r5-release` 配对备份；`0d0a79849458b4246bd597bb6a28bf1fe25beda2` 修复发布前建立 `20260920-r5-planner-fix` 配对备份。两次均在无活动 Run/Turn 时停止 Web/queue，保存数据库、成果、配置和镜像身份，验证归档及 SHA256 后发布。
- 最终修复版 public 套件通过 HTTPS、Secure/HttpOnly 会话、WSS、Origin、旧路径拒绝与自托管字体检查。规范改稿首请求的双协议 HTTP + PostgreSQL 回归通过 1 test / 8 assertions，typecheck 通过；修复后的真实公网改稿使用 Chat Completions，真实 Responses 全链依据此前 live 套件。

## 公网最终改稿复验

- 真实管家创建 Thread `a6724b3d-8128-44eb-9ca6-35bfc229f72e` 与 Task `aeed38de-126f-453e-a8d8-5dc0669dab41`，自动短标题成功。真实执行产生提问，在移动待办页回答后原 Run 完成交付，工作列表未刷新页面即更新。
- 从报告选择文字、保存批注、刷新恢复、汇总到同一对话并发送。首次真实模型未创建冻结操作，完整命令保留于历史。`0d0a798` 修复后通过 Composer 重放同一命令，Turn `ea9d99af-104c-466d-87f9-d3848e882235` 创建 accepted 改稿操作。
- 新 Run `3f9670e8-9934-42c8-a3c0-e791388e45e0` 成功，固定来源 `5ada816c-e468-4937-865b-73d980c5434a`，产生新版 `0d19b735-248e-47eb-b6f8-1ed7196be349`。新版实含“每次修改均可回看来源版本。”，其余原文保留。
- 通过页面下载两版。旧版前后 SHA256 均为 `56d07c6eaa620725e57d11d3962b5f71fb094911733e44522ed31f32d9602816`；新版为 `5c9dbc88fd8da60578fb863060c7dc52b4de41a004aaa2f88f94adfa1f804dc0`。切回旧版后返回原对话路径正确。
- 最终对话和报告浅深色、桌面及移动矩阵重新截图；页面无横向溢出。真实改稿的前次失败也保留在历史中，未伪造完成消息或删除执行记录。

## 现场收敛

- 正式运行目录统一为 cc-la `/opt/agentanywhere/runtime`；保留最终 R5 与 R4 回退源码和镜像，配对备份归入 `backups`。
- 本轮 isolated/live 栈、4 个测试 schema、临时检查目录、中间版本源码与镜像已回收；本机测试服务、隧道、工作树和临时分支已回收。原型基准分支与正式历史数据保留。
- 验收日志和下载报告归档在 `backups/20260920-r5-release/acceptance` 与 `backups/20260920-r5-planner-fix/acceptance`；目录 0700，文件 0600。

## 剩余验收

- 设置保存成功已通过；保存失败后保留编辑内容尚缺 R5 浏览器证据。TaskSpace 72 已提前结束，按 ego-browser 规则等待允许新空间后补验；#59、#60、#51 暂不关闭。
