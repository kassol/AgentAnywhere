# R1-10 提问与回答恢复验证

2026-09-18，在 cc-la `19112/19113` 隔离栈使用统一 `r1-integration-369d91c` 镜像运行 `node infra/r1/test-interaction.mjs`。Task `fcf0480b-5925-4e60-8931-94f51d3b1041`、Run `1d2bf8f2-c99f-428a-a8c7-ee857df9220f` 在 epoch 1 先提交含 0 字节附件的报告，再提出问题并进入 `waiting/cleaned`；SDK 按 Run ID 无活沙箱。重启隔离 Web 并重新登录后，原问题 ID 仍为 `pending`，空白回答返回 400 且不解除等待。另一工作可排队完成。回答后原 Run 进入 epoch 2，报告包含 `ANSWER_MARKER_13` 与先前 observation；两代各有一条 `worker.ready`，`echo_observation` 只执行一次，SDK 无活沙箱。

同一模型轮次双 `ask_user` 的 fixture 验证首问保留，第二问返回工具错误，等待界面只显示首问。另一个已提交报告后等待的 Run 被取消，公开成果 API 下载报告并匹配 SHA-256，0 字节附件可下载；旧沙箱仍由 SDK 确认为不存在。检查点恢复曾在真实环境暴露两项 SDK 边界：`readBytes({limit})` 的 limit 是行数，2454 字节 JSONL 读回 2453 字节并丢末尾换行，`readBytesStream` 读回完整 2454 字节；OpenSandbox 的权限 API 要求八进制数字文本，`0o700` 传成十进制 448 会被 execd 拒绝。集成代码现使用受限流式读取与 SDK 参数 `700/600`，本地文件权限仍用 Node 的 `0o700/0o600`。

真实浏览器 Task `383c3d0b-bd2c-46b1-abd8-e37f82b2a3de` 验证等待页刷新后问题 ID 不变、按 Enter 提交回答、epoch 2 成功报告包含 `ANSWER_MARKER_13` 与旧 observation；两代复用同一 `submit_report` 工具 ID，页面显示两张均已完成的工具卡。[恢复页面截图](r1-10-resumed.png)。SDK 按该 Run ID 无活沙箱。
