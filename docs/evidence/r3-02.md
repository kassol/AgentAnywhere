# R3-02：可靠输入与草稿恢复

实现提交：`63756b1`。在已有幂等提交上增加公开 Turn.requestId 投影；本机草稿同时保存提交身份、已创建对话、草稿修订号。实际模型调用和工作写入仍走原后端。

2026-09-20，ego-browser TaskSpace 34、本机真实 PostgreSQL/Web 与可控模型 HTTP fixture 验证：

- 现有对话与新对话草稿相互隔离，切换及刷新恢复；Shift+Enter 保留换行。
- 新对话 POST 已真实接受后，浏览器主动丢弃响应并抛出网络错误：草稿及请求身份保留；刷新自动查询原对话并恢复到原提交结果，只有一个轮次。
- 阻塞 POST 响应期间继续输入，原提交接受后新增编辑仍保留。
- 请求发出前模拟断网，刷新后显示“核对并重试”；重试成功后匹配消息与新增轮次各一条。
- 使用 Chromium IME composition 输入中文并按 Enter，轮次数保持不变；提交组合文字后仍可换行。
- 390px 输入和发送可达。[截图](r3/composer-mobile.png)。

故障注入仅位于浏览器网络返回边界；已接受的消息均由真实公开 API 写入独立数据库。没有伪造 API 回执或改写业务数据库。

聚焦 composer 与真实 PostgreSQL/API 回归 6 项通过，覆盖幂等重放、拒绝后修改、提交核对及草稿版本保护；类型、构建和 diff 检查通过。4xx 明确拒绝释放待核对状态并保留编辑；网络与 5xx 继续沿用原请求身份。固定 Craft 输入代码、许可和本地适配见 [来源记录](../r3-composer-upstream.md)。

批注提交清理与跨版本行为由 R3-06 验收；本记录不代表正式部署完成。
