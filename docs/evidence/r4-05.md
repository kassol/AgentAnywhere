# R4-05 工作与待办介入迁移证据

日期：2026-09-20  
Issue：[#47](https://github.com/kassol/AgentAnywhere/issues/47)  
固定上游：Craft Agents OSS `v0.13.3` / `e8963854c3679edcceb105a42537a06749e6cb64`

## 实现与调用链

- `src/web/Work.tsx:158-205` 继续向原 `/api/tasks`、`/api/runs/:runId/messages` 和 `/api/tasks/:taskId/runs` 发送既有 requestId/commandId；`:215-260` 继续按 Interaction/Task 标识回答、额度决定、重试和取消。迁移只改渲染和控件。
- `src/web/Work.tsx:263-340` 用 WorkStatusBadge、PermissionRequest、UserMessageBubble、Craft 表单控件和 EntityRow 组合详情、待办、对话、创建与列表。Task 直接链接不变；成果与版本路径仍使用持久 Artifact Version。
- `src/web/WorkStatus.tsx:3-34` 分开工作状态和 Interaction 状态；工作 `running` 明确为“执行中”。未知服务端状态原样显示，便于诊断。
- `src/web/Steward.tsx:41-75` 只为最新真实 Run/Interaction/Version 生成追加、取消、回答、额度、重试和改稿动作；`:317-346` 用 EntityRow/StatusBadge 显示关联工作和状态卡。成果链接与按钮位于 EntityRow children，未嵌套到主链接。
- `src/web/main.tsx:107-114,172-179` 的全局待办继续读取 `/api/interactions/pending` 同一 Interaction，并用 EntityRow 呈现；管家入口的 QuickActions 只把完整 Task/Interaction 命令填入 Composer。
- `src/web/QuickActions.tsx:16-25,42-75` 保留命令中的完整 Task/Run/Interaction/Version/Operation 标识。`Steward.tsx:247-253` 在覆盖非空草稿前沿用确认，确认取消时不改草稿；工作取消和 `Steward.tsx:256-264` 的停止管家轮次仍是两个独立操作。
- `src/web/StewardReceipts.tsx:48-112` 用 EntityRow/StatusBadge 渲染回执，同时显示完整持久标识；继续回执仍按原 Operation 精确填入。`style.css` 已删除被替代的旧 work/status/receipt/related-card 规则，工作专用布局收敛到 `work.css`。

Radix Select 不冒泡原生 `change`。`Work.tsx:305-309,336-342` 因此在继续工作和创建工作的模型/协议变更时显式轮换各自 requestId，保持请求负载变化时的幂等语义。
`Work.tsx:31-37` 从首次空值起始终向 Radix Select 传递受控字符串，并忽略异步注册选项期间 BubbleSelect 产生的非选项空值；默认模型与用户选择均只接受当前模型列表内的值。

## 自动验证

```text
$ bun test src/web/WorkStatus.test.ts src/web/QuickActions.test.ts src/web/StewardQuickActions.test.ts src/web/StewardReceipts.test.ts src/web/Work.test.tsx
13 pass
0 fail
42 expect() calls

$ bun run typecheck
$ tsc --noEmit

$ bun run build
✓ built
```

回归覆盖异步默认模型同步、用户模型选择、工作 `running` 标签、未知状态、各类精确快捷命令、最新 Run/Interaction 限制、回执轮次归属和安全 Markdown。真实浏览器证据见下文；发布级兼容与隔离回归统一记入 R4-08。

## 真实浏览器验收

使用 ego-browser 唯一 TaskSpace 46，固定构建前端连接隔离真实网关栈，未改正式业务数据。

- 工作页创建 Task `d1d40f02-df34-4280-bc3a-30129b1c7173`，Run `0eee50b7-0cfd-45d7-85c3-ee13f1adc8e7` 实际调用 `ask_user` 后等待。全局待办与工作详情共同指向 Interaction `c6b8bac5-94e0-4c02-baaf-55f04f592f39`。
- 从全局待办进入详情，填写“R4 原组件确认”并提交，原 Interaction 变为 answered，原 Run 恢复并成功完成，沙箱 cleaned。报告 Version `ddb9ea98-1903-4cdb-abf6-a26583cc0764`，SHA-256 `058d80eed34a0c5de79f28153d0575c075250b49f5d4eac5b5601a69ac779514`。
- 非空草稿下点击“填写改稿要求”，原生覆盖确认的取消分支保留“正在组合的新草稿”；确认分支填入完整 Task `e3b6c946-5a32-4fbb-a9b8-0d41133bab54` 与 Version `f3a31dfe-4076-41a6-b58a-3bb9fc6bebff`。页面仍为 5 轮，没有自动发送。
- 初次浏览器发现默认模型在 Radix 异步选项注册时被空回调覆盖；已修复；在包含修复的固定构建中重新进入工作页，实际读取模型为 `gpt-6-astra`，创建按钮 `disabled=false`。
- 工具处理原生确认框曾超时；在同一 TaskSpace 关闭无响应测试页后恢复，后续确认/取消与草稿读取完成。超时尝试不计入通过证据。
