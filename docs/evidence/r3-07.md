# R3-07：分层模型设置

实现 `ceb123c`，保留既有模型连接契约。日常层显示管家模型、协议与人工调研池；原生可折叠详情包含网关、凭证、发现、参考目录、协议、来源、映射与人工覆盖值。

2026-09-20，ego-browser TaskSpace 34、本机真实 Web/API 验证：

- 修改管家协议并保存后，模型默认协议及其余覆盖字段完整保留；恢复原管家协议成功。
- 键盘 Enter 可展开连接详情；密钥输入始终为空，公开 API 不回显密钥。
- 修改输出上限而不保存，再保存连接：未保存字段仍保留，服务端值尚未变化；保存全部模型后 API 才更新为新值，随后恢复原值。
- 1440px 浅色日常层与 390px 深色模型详情可用，无横向溢出。

[桌面截图](r3/settings-desktop.png)、[窄屏详情](r3/settings-mobile.png)。`ModelSettings.test.ts` 2 项通过；`model-connection.test.ts` 4 项、126 次断言通过，覆盖协议连接、持久化、刷新失败保留、凭证保护、能力来源/撤销覆盖/映射、管家与调研池校验。类型、构建、diff 检查通过。

固定 Craft 来源、许可与依赖适配见 [设置来源记录](../r3-settings-upstream.md)。在途配置继续由既有 Run/Turn 快照固定，未改变模型选择与授权契约。
