# R4-03 工作台导航与主题迁移证据

日期：2026-09-20

Issue：[#45](https://github.com/kassol/AgentAnywhere/issues/45)

固定上游：Craft Agents OSS `v0.13.3` / `e8963854c3679edcceb105a42537a06749e6cb64`

## 实现结果

- `src/web/craft/components/Panel.tsx` 保留上游 Panel 的 grow/shrink、最小宽度与 overflow 容器，并用语义 `as` 适配浏览器 `aside/main`。
- `src/web/craft/components/SidebarButton.tsx` 保留上游图标、标题、label、active/ghost 与焦点结构；浏览器端使用原生链接，全部主导航、对话和待办入口继续支持直接访问、刷新、历史和新标签页。
- `src/web/main.tsx` 实际以 Panel 组成 A 的侧栏和主面板，以 SidebarButton 渲染页面、对话和待办导航；保留 252px 桌面侧栏、按需报告预览及小屏单面板结构。
- `src/web/craft/components/Empty.tsx` 保留上游空态组合，并用于无历史对话的侧栏空态。
- `src/web/craft-theme.css` 接入固定上游六色与派生 surface/token；`theme.ts` 保留浅色、深色、跟随系统，并同步 `.dark`、`color-scheme` 与 `data-font="inter"`。字体继续来自 `src/web/fonts/InterVariable.woff2`，中文使用系统回退。
- 已删除被 SidebarButton 替代的旧导航项样式，以及已由 TurnCard/ActivityRow 替代且无消费者的 `.tool-activity*` 和 `tool-pulse`；`tool-activities` 布局与 `return-latest` 仍有实际消费者，继续保留。

来源逐段对照见 [固定来源与迁移清单](../r4-craft-source.md)。

## 自动验证

```text
$ bun run typecheck
$ tsc --noEmit

$ bun run build
vite v6.2.4
✓ 2387 modules transformed.
✓ built in 1.90s

$ bun test src/web/WorkPreview.test.tsx src/web/ActivityFeed.test.tsx src/web/Composer.test.ts
16 pass
0 fail
40 expect() calls
```

Vite 只报告现有单包超过 500 kB 的提示。构建产物包含自托管 `InterVariable.woff2`；Tailwind 产物已生成 `ring-ring`。桌面/小屏、三主题、键盘焦点、长标题、空态和字体 computed style 由主线程用 ego-browser 补充验收。


## 浏览器验收

- ego-browser TaskSpace 46，在隔离live API上验证。桌面2048×1194与小屏390×844均无页面横向溢出；小屏四个主导航均为44px高，输入框位于可视区域。
- 深色、浅色实际切换；跟随系统在 Chromium 模拟系统明暗变化后同步切换，最终恢复浅色与正常系统媒体。Inter 字体加载成功，computed font保留Inter与中文系统回退。
- Tab从品牌到管家导航，原SidebarButton显示有效1px内环，原生链接可操作。长对话标题截断；已有对话、新对话空态、工作、设置和报告入口均可达。
- 小屏从管家打开原报告后显示单面板，“返回对话”恢复原输入；新建对话恢复独立new草稿。直接链接和刷新已在输入与报告链路验证。
- 浏览器发现Panel flex导致旧页宽度收缩，以及Button固定高度截断主题选项；显式全宽与主题auto高度修复后重新检查，主题文字完整且可点击。
- 中间截图位于本轮临时目录，最终全页面视觉证据由 #50 统一归档。
