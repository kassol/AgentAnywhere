# R2-11：长对话摘要与统一预算

实现 `783df148c8e09f0b1470f9aa6c33fbfff91c831a`；Issue #29。

接近固化模型上下文上限时压缩较早轮次，保留全部原消息和当前工作事实；页面展示当前对话内的覆盖轮次。摘要仅作上下文，不能授权操作。摘要请求与普通请求共用 8 次请求、5 分钟活跃时间及 3 项工作创建预算；第八次响应中的工具不能再发起新操作。已接受业务回执保持可重放。

## 验证

- 独立复核修正单个长旧轮次无法摘要、覆盖范围误用全局序号两项问题。摘要请求前及摘要完成后检查上下文，超限则明确失败并保留原文。统一预算在 turn 行锁下执行，已接入调研派发、工作控制和回答事务；重试与改稿合入时复用同一检查。
- 摘要与第八次响应工具限制的两个公开 API 回归通过。其余管家目标回归通过；派发恢复测试改为只统计当前用户消息后的工具结果，使用外部 HTTP gate 确认阻塞后停服，定向回归 33 断言通过。类型检查、构建与 diff 检查通过。
- 停服入口先同步关闭管家调度并 abort，再等待 HTTP 和数据库连接关闭。
- cc-la 隔离运行 `node infra/r1/test-steward-summary.mjs` 通过，两种协议各输入 3 条 14000 字符消息，自动累计覆盖第 1–2 轮；第三轮实际模型请求为 3 次（摘要、规划、回答），全部原消息保持一致。Web 重启后摘要 ID、原消息和请求计数保持。
- Chat 对话：`b7ca4829-e279-45b3-8ae8-0497474bd9fb`，摘要 `e9447cbd-b01a-4f7b-bd92-e5b619acafc1`。
- Responses 对话：`7dc0d65a-ef1f-4488-b827-b3d4f43c1d28`，摘要 `59dce5cd-fd60-49bf-8fcc-ea649e8c743a`。
- ego-browser TaskSpace 32 打开 Responses 对话，显示“覆盖本对话第 1–2 轮，共 2 轮”；键盘发送第四条中文讨论消息后刷新，摘要及 4 条原用户消息仍在。截图 `r2-11-summary.png` 已检查。

本票使用真实 Web/API 和 PostgreSQL、可控模型 HTTP；真实网关全量回归与正式发布在 #30 汇总。
