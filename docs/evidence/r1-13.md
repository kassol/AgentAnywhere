# R1-13：完成后继续工作

## 已验证

- 在独立 PostgreSQL schema 上通过公开 Web/API：已完成 Task 在原 Thread 创建新 Run；新 Run 固定当前模型端点与协议，关联上一版报告，重复请求幂等，并发创建只允许一项进入队列。
- 新 Run 失败后，旧报告版本仍可通过公开 API 读取；新 Run 创建后旧版本不被覆盖。
- `bun run typecheck`、`bun run build`、`bun test` 与独立 PostgreSQL 的 `src/work.test.ts` 通过。

2026-09-18，cc-la 隔离 `19112/19113` 栈以统一 `r1-integration-369d91c` 镜像运行 `node infra/r1/test-continuation.mjs`。Task `e503ccb0-a3af-42cd-8b9f-3206e3bf014a`、Thread `cb730563-58a7-4495-8d85-a9cf673307d8` 共创建 3 个 Run、保留 2 个报告版本。第二个 Run 的模型请求收到上一版报告与本次修改要求，修订报告包含 `CONTINUATION_MARKER_16`；第三个 Run 失败后两版报告仍按 SHA-256 原样下载。Web 重启后数据仍可读，SDK 按三个 Run ID 查询均无活沙箱。

真实浏览器已打开上述 Task，切换两版报告并下载：新版 162 字节、旧版 138 字节，文件 SHA-256 与 ArtifactVersion 一致；第三个 Run 失败后旧版仍可读。Markdown 渲染后的 DOM 中无 `script`、`img` 或 `javascript:` 链接。[历史版本截图](r1-13-history.png)。浏览器新建继续 Run 的完整路径仍待验。
