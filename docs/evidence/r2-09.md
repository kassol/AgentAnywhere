# R2-09：明确报告改稿

实现 `65d94fc`；验收脚本修正 `8ac44f5`；Issue #27。

完整改稿命令冻结用户修改原文、唯一 Task、来源报告版本和池内模型。原工作创建新 Run，业务写入、关联与回执同事务提交；重复提交和明确恢复返回原回执。多版本必须提供版本 UUID。失败的新执行保留旧报告。

## 验证

- Sol 实现并独立复核，主线程复读冻结、执行与 `continueTaskInTransaction` 调用链。类型检查、构建、work 105 断言、steward 115 断言和差异检查通过。
- cc-la 隔离应用 `260dc831ec2a6a7d219bee0a2972d6e65ed11ef5` 执行修正后的 `test-steward-revision.mjs` 通过。内部 Web/API、PostgreSQL、队列、Pi、Sandbox 真实，仅模型 HTTP 可控。
- 覆盖普通解释、引用指令、多目标拒绝、多版本缺少版本拒绝；成功改稿、重复请求、停止后明确恢复；真实失败 Run；应用重启后两版报告下载字节一致；三个 Run 经 SandboxManager 查询无存活沙箱。
- 工作 `49b01a28-3116-4fd1-ae87-e279ed1594eb`；源版本 `64e20554-b71d-4ce4-9cf9-72fd84ba3c6d`；改稿版本 `547ced69-2c0b-45d6-9719-0d5a6da8a367`；回执 `def6e253-a561-436b-8bc2-146304153651`；成功 Run `5291cca3-f9cc-4e89-b12e-7ef4b25215c5`；失败 Run `21055b96-a9cc-4611-b0e8-1f126a223a55`。
- 初次断言把成功改稿的附件也纳入报告版本集合；实际数据库确认附件类型后，测试按 `kind=report` 检查两版及其成功 Run，保留完整字节校验。
- ego-browser TaskSpace 32 通过中文键盘发送带明确源版本的改稿命令；新 Run `e8ca5a24-90df-435f-b037-f6c06e6bc04a` 成功并回收，生成版本 `9319d6f7-3efd-4ddf-be23-3a2a7e3202fa`。刷新后原两版与新版本均可见，回执展示来源版本、模型与新 Run。截图 `r2-09-revision.png` 已检查。

真实网关整体流程和正式发布在 #30 记录。
