# R5 实现复核

范围：`858b451` 起的 R5 实现。由两个独立 Sol agent 并行按 Standards 与 Spec 复核，主线程读取实际调用路径并修正；浏览器与发布验收另见 R5-09。

## Standards

- 已修复：人工将对话命名为“新对话”后，首条消息的摘要回退会覆盖人工标题。`bad1442` 为同一 SQL 更新补充 `NOT title_edited`，公开 API 回归先复现失败，再验证通过。
- 标题 PATCH 的局部重复与页面判断的分散属于判断性建议。本轮保留现有组织，遵循项目的外科修改规则；新页面路径均有认证路由回归。
- 未发现新增凭证泄露、数据丢失或 Craft 来源许可遗漏。

## Spec

- 已修复：`/reports` 缺少服务端 HTML 路由；小屏报告缺主导航；报告标题没有使用人工标题。路由回归先得到 404，再验证认证后的 200 与匿名重定向。
- 已修复：独立报告返回时丢失来源。`f747306`、`658ccaf` 将受限本地来源路径保存在 URL，切换版本保留来源；从对话进入时，批注汇总写回同一对话草稿。跨站来源不接受。
- 已修复：设置页遗漏 A 的会话列。所有已登录页面使用同一三栏外壳，小屏使用导航菜单。
- 已修复：已关联操作回执归入对应 Task 的展开历史；尚未确定 Task 的操作仍按原轮次保留。回归验证同一回执不会因恢复轮次重复显示。

Standards 确认的 1 项缺陷已修复；Spec 窄审查与正式复核发现的页面缺口已修复。是否完成发布以 R5-09 的真实执行和浏览器证据为准。

## 最终浏览器复核修正

- `e699fc5`：深色登录输入框仍使用浏览器白底黑字；补齐共享 Input/Textarea 的局部重置和 muted 背景映射。浏览器读取计算样式确认继承深色前景，浅深截图重新生成。
- `b380a89`：工作列表停留时保留旧状态；详情使用的既有 refresh 同样用于列表轮询，保持创建表单输入独立。
- `2eaf405`：工作/待办小屏导航受通用 content 内边距影响，只占 358px；复用设置页的全宽规则，390px 视口测得页头与页面均为 390px。

- `0d0a798`：公网批注命令合法，但真实模型在 planner 首请求直接文本结束，未创建改稿操作。仅对已有规范命令解析通过的请求指定首个冻结工具，后续恢复自动选择；双协议公开 API + PostgreSQL 回归先红后绿（1 test / 8 assertions），原文、版本、模型池与预算校验保留。解释及引用式命令继续由既有入口拒绝。
