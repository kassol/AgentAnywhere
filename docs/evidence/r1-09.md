# R1-09 执行中追加要求验证

2026-09-18，cc-la 的隔离 R1 环境以 `r1-09` 镜像运行 Web、queue、Pi 沙箱和模型 HTTP fixture。`node infra/r1/test-steering.mjs` 通过公开 Web/API 创建 Task `e8ac7e2f-5b00-4d9f-bfff-9308b248a4e2`、Run `e56d963e-63cd-4e1d-81b5-d78d046e7f8c`。在最终响应流式输出期间追加命令：首次提交得到 `201/pending`，相同 `commandId` 重提得到 `200`，工作详情仅有一条追加消息。fixture 记录四次模型请求：前三次无追加要求，第四次包含 `STEERING_MARKER_12`；Run 最终 `succeeded/cleaned`，追加消息为 `applied`。OpenSandbox SDK 按 Run ID 查询无存活沙箱。

本地独立 PostgreSQL 测试 schema 的公开 API 回归覆盖消息刷新后可见、并发同 ID 提交仅新增一次、重复确认幂等、错误 token 和旧 epoch 不能确认待处理消息、完成前未确认的消息保留 `pending`。`bun run build`、`bun run typecheck` 和含数据库的 `bun test` 均通过（8 pass，183 assertions）。

工具运行中追加的完整时序仍待 #11 接通真实耗时搜索工具后验证。当前流式回归仅证明已发请求不被改写、下一模型请求收到追加要求；尚未将 #12 视为全部验收完成。
