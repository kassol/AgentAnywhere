# R1-09 执行中追加要求验证

2026-09-18，cc-la 的隔离 R1 环境以 `r1-09` 镜像运行 Web、queue、Pi 沙箱和模型 HTTP fixture。`node infra/r1/test-steering.mjs` 通过公开 Web/API 创建 Task `e199138a-171a-4ffd-ac9b-08ac47845140`、Run `cfd842a6-d07c-47f8-a8b5-09113b9fd093`。在最终响应流式输出期间追加命令：首次提交得到 `201/pending`，相同 `commandId` 重提得到 `200`，工作详情仅有一条追加消息。fixture 记录五次模型请求：前三次无追加要求，第四次包含 `STEERING_MARKER_12` 并再次调用 `submit_report`，第五次收到修订报告的工具结果。公开成果 API 返回含标记的修订报告；Run 最终 `succeeded/cleaned`，追加消息为 `applied`。OpenSandbox SDK 按 Run ID 查询无存活沙箱。新报告先写入独立 generation 目录，稳定 manifest 指针在写完后原子替换；queue 按指针固定的代次复制、校验并保存成果。旧代文件留在沙箱至回收。

改造前的流式回归 Task `e8ac7e2f-5b00-4d9f-bfff-9308b248a4e2`、Run `e56d963e-63cd-4e1d-81b5-d78d046e7f8c` 已证明追加要求进入后续模型请求；报告仍只有首个版本，因此引入上述修订路径。

本地独立 PostgreSQL 测试 schema 的公开 API 回归覆盖消息刷新后可见、并发同 ID 提交仅新增一次、重复确认幂等、错误 token 和旧 epoch 不能确认待处理消息、完成前未确认的消息保留 `pending`。`bun run build`、`bun run typecheck` 和含数据库的 `bun test` 均通过（8 pass，183 assertions）。

真实浏览器还验证了两个界面结果。Task `85e8f598-7208-417b-b286-8fd34cff723e` 在流式阶段追加 `STEERING_MARKER_12`，先显示“待处理，将在下一模型步骤生效”；刷新后消息已处理，最终报告正文包含标记。[工作详情截图](r1-09-steering.png)、[报告正文截图](r1-09-report.png) 均经目视确认。另一次流式末尾的完成竞态中，消息成功持久化，但 Run 已结束；页面明确显示待处理，刷新后仍保留。工具运行中追加的完整时序仍待 #11 接通真实耗时搜索工具后验证。当前流式回归证明已发请求不被改写、下一模型请求收到追加要求并修订报告；尚未将 #12 视为全部验收完成。
