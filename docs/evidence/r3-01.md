# R3-01：A 工作台与主题

实现提交：`02fa904`。采用已确认 A 经典侧栏，真实对话历史归入左侧，中央对话占满可用高度。工作、待办、设置继续使用原路由；外壳不包含演示数据、方案切换器或 Electron 运行依赖。

2026-09-20 使用 ego-browser TaskSpace 34，在本机独立 PostgreSQL 与 Web 实例验证；模型响应使用明确隔离的 HTTP fixture。浏览器集成了 R3-02 的输入框及重复历史栏移除，本记录仅验收外壳与主题。

- 1440×900：既有真实对话重开，左侧导航与历史可用，工作和设置入口可访问。
- 390×844：无横向溢出；输入框底边 775.5px，发送按钮可达；深色下控件与焦点可辨识。
- 浅色、深色选择刷新后保留。跟随系统时，在管家页改变系统配色立即更新主题。
- 首轮发现并修复历史栏位置偏离 A、系统主题仅在设置页监听两项问题，复验通过。
- `bun run typecheck`、`bun run build` 与 diff 检查通过。`src/server.test.ts` 中登录、匿名拒绝、退出、跨 Origin、限流和 HTTPS 代理检查通过；代理检查另行按名称核验。

截图：[桌面](r3/shell-desktop.png)、[窄屏深色](r3/shell-mobile.png)。固定上游、许可、提取内容及依赖边界见 [Craft 来源记录](../r3-craft-source.md)。

本票不代表 R3 全部交互或正式发布已验收；统一结果由 R3-08 记录。
