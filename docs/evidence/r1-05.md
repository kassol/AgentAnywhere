# R1-05 最小执行链路验收

2026-09-18，在 cc-la 的 R1 环境验收。正式 Web 使用 gVisor，PostgreSQL 不暴露主机端口；gVisor 内无法解析 Docker 服务名 `postgres`，部署脚本从现有 R1 容器读取当前内网 IP，只给 Web 的 `/etc/hosts` 映射该名称。保留原数据库 DSN 和网关解析配置。正式 Web、queue、PostgreSQL 均运行，Web 健康检查通过。正式 Web 从受限模型配置读取 `gpt-6-astra`/Chat Completions；刷新真实网关模型列表与 models.dev 后，使用保存的同一模型和协议调用 sub2api，连接测试返回 200。真实目录字段来自 [R1-03](r1-03.md)，不是手填假数据。

隔离 fixture Web、queue 使用独立 PostgreSQL schema、数据目录和 loopback 端口；模型 HTTP 是唯一可控边界。`node infra/r1/test-run.mjs` 创建 slow、mixed、split 三项工作，真实 pg-boss 串行派遣、OpenSandbox 创建 gVisor 沙箱、Pi 0.85.1 发起两轮 Chat Completions 并调用 echo 工具。三项均 `succeeded` 且 `cleanup=cleaned`；测试读取 fixture `/calls` 核对工具入参与真实返回文本，公开事件 API 核对消息、事件 ID、游标重放和混合未知用量。split 场景把 SSE 的 CRLF 拆在两个 TCP 块，最终用量仍被读取；非法 JSON `null` 返回 400。OpenSandbox SDK 按每个 Run ID 查询均无存活沙箱。

浏览器在 `127.0.0.1:19112` 的隔离 Web 看到 slow 场景的中间流式文本；刷新后只显示一条工具结果和一条最终消息，输入/输出 20/10 token、实际费用未知。[浏览器截图](r1-05-chat.png)。mixed 场景刷新后输入/输出均显示未知。两次模型调用中，只要任一轮网关没有 usage，总量保持未知。

真实 sub2api Pi 测试另用隔离 `r105_live` schema 与 `127.0.0.1:19114` Web。正式 `0600` 模型文件只读挂载，未复制密钥或修改正式 Task/Run。`gpt-6-astra` Chat Completions 的 Task `e1228094-6892-4f66-83d2-0baa64fc4e80`、Run `a11bab33-ac76-4f9f-8017-a90ce449c190` 完成真实 echo 工具往返，20 条事件持久化，状态 `succeeded`、清理 `cleaned`；两轮网关 usage 分别为输入/输出 448/21、484/14 token，SDK 查询无存活沙箱。`test-live-connection.mjs` 与 `test-live-run.mjs` 可重现相应检查，执行前需有上述隔离配置。

在同一隔离 schema，将测试 queue 的 Agent 镜像暂指向不含 Pi 入口的 fixture 镜像；Task `37d519cd-8a9f-4d3f-9aba-681049f9bf38` 的 Run `99bc51de-7cb9-4251-9643-b980cc403995` 在约 20 秒内落为 `failed`，`cleanup=cleaned`，无 `worker.ready` 事件，SDK 查询无存活沙箱。测试后 queue 已恢复正常镜像。`test-startup-failure.mjs` 保留此回归。

本地 `bun run build`、`bun run typecheck`、真实 PostgreSQL 临时 schema 上 `bun test` 均通过（8 pass，161 assertions）。本票执行链路只覆盖 Chat Completions 和无外部副作用的 echo 工具。Responses、搜索、取消/追加、报告与审批仍待后续票；正式公网入口未切换。
